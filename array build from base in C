#include<stdio.h>
#include<stdlib.h>
struct array{
   int A[20];
   int size;
   int length;
};
void display(struct array Arr){
    printf("elements are:");
       for(int i=0;i<Arr.length;i++){
    printf("%d",Arr.A[i]);
   }
}
int main(){
   struct array Arr={{1,2,3,4,5},20,5};
   display(Arr);
   return 0;
}
